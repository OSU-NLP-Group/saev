[project]
name = "trait-discovery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandera>=0.26.1",
    "tabulate>=0.9.0",
]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pyinstrument>=5.1.1",
]
